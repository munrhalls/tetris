// gets rows number with clear class, clears every cell with that of all stuff
const lineClearer = {
  clearRows: function clearRows(rows) {
    console.log(rows);
  },
};

export default lineClearer;
